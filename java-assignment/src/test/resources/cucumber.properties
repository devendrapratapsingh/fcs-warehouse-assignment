# Cucumber JUnit Platform Engine configuration
# This file is picked up automatically by the cucumber-junit-platform-engine.

# Use PicoContainer for dependency injection between step definition classes.
# This enables ScenarioContext to be shared across CommonSteps, WarehouseSteps,
# FulfillmentSteps etc. within the same scenario — each scenario gets a fresh instance.
cucumber.execution.parallel.enabled=false
